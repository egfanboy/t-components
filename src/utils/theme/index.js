const primary = Object.assign('#5707eb', { light: '#6217ed', dark: '#4a0072' });

const error = Object.assign('#d32f2f', { light: '#e54242', dark: '#9e0707' });

const success = Object.assign('#30a64a', {
    light: '#42b45b',
    dark: ' #178b30',
});

const warning = Object.assign('#ff9926', {
    light: '#ffa726',
    dark: '#ff8800',
});

export default {
    primary,
    error,
    warning,
    success,
};
